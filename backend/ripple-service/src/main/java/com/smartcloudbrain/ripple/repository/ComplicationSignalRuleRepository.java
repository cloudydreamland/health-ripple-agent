package com.smartcloudbrain.ripple.repository;

import com.smartcloudbrain.ripple.entity.ComplicationSignalRule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ComplicationSignalRuleRepository extends JpaRepository<ComplicationSignalRule, Long> {
}
